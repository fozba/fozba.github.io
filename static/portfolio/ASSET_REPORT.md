# Portfolio visualization asset report

Generated by `scripts/portfolio/generate_plot_assets.py`.

Plotly is not embedded in any portfolio asset. The page supplies one pinned shared runtime.

| Experience | Before raw | Before gzip | After data raw | After data gzip |
| --- | ---: | ---: | ---: | ---: |
| basin | 685.3 KiB | 359.7 KiB | 330.8 KiB | 193.9 KiB |
| nevada | 2566.0 KiB | 1163.6 KiB | 456.6 KiB | 299.4 KiB |
| forge | 5726.7 KiB | 1948.9 KiB | 164.2 KiB | 81.3 KiB |
| monte-carlo | 4927.9 KiB | 1521.3 KiB | 4.2 KiB | 1.6 KiB |
| tornado | 4746.1 KiB | 1434.5 KiB | 3.1 KiB | 1.3 KiB |
| sierra model HTML → shared-runtime module | 19.7 KiB | 5.5 KiB | 34.1 KiB | 9.8 KiB |

Data-only total (five exported figures): **958.9 KiB raw / 577.4 KiB gzip**.

The old combined total includes three duplicate embedded Plotly runtimes. The compact total excludes the shared runtime because the parent downloads it once.
The common data adapter is **6.5 KiB raw / 2.4 KiB gzip** and is shared by all five plots.

## Scientific/display transformations

- Basin retains all 20,000 observations and all 45 formations.
- FORGE retains all 2,900 microseismic events and every stage trace; only smooth interpolated line samples are reduced.
- Nevada stores 17,436 shared coordinates once and removes 52,308 preformatted hover strings. Hover text is regenerated accessibly at runtime.
- Nevada's signed prediction-minus-PFA layer uses a scientifically meaningful −1 to +1 diverging scale centered on zero; the old export clipped negative values with a 0 to 1 scale.
- Monte Carlo retains exact P90/P50/P10 values and all 10,000 outcomes as counts across 100 bins; raw samples are not sent to the browser.
- Tornado retains all seven variables and exact sensitivity values.
- Float32 conversion affects display coordinates only; maximum absolute errors are recorded in `asset-size-report.json`.
